# Machine Learning A-Z (2026) — Progress Log

Notebooks completed so far while working through **[Machine Learning A-Z: ML, DL, AI with AWS, Python & R](https://www.udemy.com/course/machinelearning/)** on Udemy. All notebooks were built and run in Google Colab.

Each `.ipynb` is a trained model on sample data, meant to double as a reusable **template** for that algorithm.

---

##  Data Preprocessing
| Notebook | Description |
|---|---|
| `data_preprocessing_template.ipynb` | Reusable template for cleaning and preparing raw datasets |
| `data_preprocessing_tools.ipynb` | Core preprocessing tools — handling missing data, encoding categorical variables, feature scaling, train/test split |

##  Regression
| Notebook | Description |
|---|---|
| `simple_linear_regression.ipynb` | Predicting a target using a single feature |
| `polynomial_regression.ipynb` | Fitting non-linear relationships with polynomial terms |
| `decision_tree_regression.ipynb` | Tree-based regression model |
| `random_forest_regression.ipynb` | Ensemble of decision trees for regression |

##  Classification
| Notebook | Description |
|---|---|
| `logistic_regression.ipynb` | Binary classification using logistic regression |
| `k_nearest_neighbors.ipynb` | Classification based on nearest data points (KNN) |
| `support_vector_machine.ipynb` | SVM classifier for linear/non-linear boundaries |
| `kernel_svm.ipynb` | SVM using kernel trick for non-linear separation |
| `naive_bayes.ipynb` | Probabilistic classifier based on Bayes' theorem |
| `decision_tree_classification.ipynb` | Tree-based classification model |
| `random_forest_classification.ipynb` | Ensemble of decision trees for classification |

##  Clustering
| Notebook | Description |
|---|---|
| `k_means_clustering.ipynb` | Unsupervised clustering using K-Means |
| `hierarchical_clustering.ipynb` | Clustering via dendrograms and linkage methods |

##  Dimensionality Reduction
| Notebook | Description |
|---|---|
| `principal_component_analysis.ipynb` | PCA for reducing feature dimensionality |
| `linear_discriminant_analysis.ipynb` | LDA for dimensionality reduction with class separability |
| `kernel_pca.ipynb` | Non-linear dimensionality reduction using kernel trick |

##  Model Selection & Boosting
| Notebook | Description |
|---|---|
| `k_fold_cross_validation.ipynb` | Evaluating model performance using k-fold CV |
| `grid_search.ipynb` | Hyperparameter tuning via grid search |
| `xg_boost.ipynb` | Gradient boosting model using XGBoost |

---

##  Notes
- All notebooks are stored and versioned via Google Colab.
- Templates (`data_preprocessing_template.ipynb`) are kept generic so they can be reused as starting points for new datasets.
- This log will be updated as more sections of the course (Deep Learning, AWS deployment, etc.) are completed.

##  Status
**Completed:** Data Preprocessing → Regression → Classification → Clustering → Dimensionality Reduction → Model Selection/Boosting
**Next up:** Deep Learning & AWS modules
